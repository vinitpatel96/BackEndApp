<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Weather</title>
    <link rel="stylesheet" type="text/css" href="/css/style.css">
    <link href='https://fonts.googleapis.com/css?family=Open+Sans:300' rel='stylesheet' type='text/css'>
  </head>
  <body>
    <div class="container">
      <h1>What's the Weather in your City like?</h1>
      <fieldset>
        <form action="/" method="post">
          <input name="city" type="text" placeholder="Enter your City" required>
          <input type="submit" value="Get Weather" class="button">
        </form>
        <% if(weather !== null){ %>
          <p><%= weather %></p>

        <% } %>

        <% if(error !== null){ %>
          <p><%= error %></p>
        <% } %>
      </fieldset>
    </div>
  </body>
</html>